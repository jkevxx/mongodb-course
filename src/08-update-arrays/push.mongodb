use('platzi_store');

db.inventory.updateOne(
  { _id: 4 },
  {
    $push: {
      tags: 'headphone',
    },
  }
);

// add several elements
db.inventory.updateOne(
  { _id: 4 },
  {
    $push: {
      tags: {
        $each: ['headphone', 'wireless'],
      },
    },
  }
);
